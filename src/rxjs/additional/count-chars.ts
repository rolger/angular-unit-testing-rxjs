import {EMPTY, Observable} from "rxjs";

/*
    Count the number of occurrences of each character in a given string and return it as a list of tuples in order of appearance.
    For empty output return an empty list.
 */
export class CountChars {

    countWithRxJs(input: Observable<any>) {
        return undefined;
    }

}